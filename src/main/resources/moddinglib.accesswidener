accessWidener v2 named

# Used by the current fast video upload path in VideoComponent. Memcpy
# natively from the FFmpeg scaling buffer directly into the NativeImage's
# GPU-bound memory, skipping the per-pixel setColorArgb JNI loop.
accessible field net/minecraft/client/texture/NativeImage pointer J
